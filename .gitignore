# Data volume. DATA_DIR is relative by default, so it lands wherever you run the CLI —
# ignore it anywhere rather than only at the repo root.
data/
!data/.gitkeep

.env
.env.local

__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.hypothesis/

node_modules/
dist/
web/src/lib/api.d.ts    # generated by `npm run gen:api`
web/tsconfig.tsbuildinfo
